/**
 * Platform name registered with Homebridge. Must match the "platform"
 * value used in the Homebridge config.json.
 */
export const PLATFORM_NAME = 'Tricom';

/**
 * npm package name. Must match the "name" field in package.json.
 */
export const PLUGIN_NAME = 'homebridge-tricom';
